instale o Node.js antes de clonar o repositório

clone o repositório no vscode

aperte ctrl + shift + aspas pra abrir um terminal novo
execute o comando: npm i

reinicie o vscode

pra iniciar o projeto e aparecer o codigo qr:

veja se o celular e o computador estão conectados na mesma rede.
o qr code é local, então se não estiver na mesma rede, não vai conectar

execute o comando: npx expo start

depois disso vai aparecer o qr code. é só escanear e codar :)
